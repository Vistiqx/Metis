diff --git a/frontend/src/components/layout/RightPanel.tsx b/frontend/src/components/layout/RightPanel.tsx
index 2a5363b..1085fb6 100644
--- a/frontend/src/components/layout/RightPanel.tsx
+++ b/frontend/src/components/layout/RightPanel.tsx
@@ -1,15 +1,19 @@
-import { X, ChevronRight, ChevronLeft } from 'lucide-react'
-import { useState } from 'react'
-import { Badge } from '../ui/Badge'
+import { X, ChevronRight, ChevronLeft } from "lucide-react";
+import { useState } from "react";
+import { Badge } from "../ui/Badge";
 
 interface RightPanelProps {
-  children?: React.ReactNode
-  title?: string
-  onClose?: () => void
+  children?: React.ReactNode;
+  title?: string;
+  onClose?: () => void;
 }
 
-export function RightPanel({ children, title = 'Details', onClose }: RightPanelProps) {
-  const [collapsed, setCollapsed] = useState(false)
+export function RightPanel({
+  children,
+  title = "Details",
+  onClose,
+}: RightPanelProps) {
+  const [collapsed, setCollapsed] = useState(false);
 
   if (collapsed) {
     return (
@@ -21,11 +25,11 @@ export function RightPanel({ children, title = 'Details', onClose }: RightPanelP
       >
         <ChevronLeft className="h-5 w-5" />
       </button>
-    )
+    );
   }
 
   return (
-    <aside className="hidden h-full w-[320px] flex-col border-l border-border/70 bg-card/80 backdrop-blur xl:flex">
+    <aside className="hidden h-full w-[296px] flex-col border-l border-border/70 bg-card/80 backdrop-blur xl:flex">
       <div className="border-b border-border/70 px-5 py-4">
         <div className="mb-2 flex items-center justify-between">
           <div>
@@ -44,14 +48,19 @@ export function RightPanel({ children, title = 'Details', onClose }: RightPanelP
             <ChevronRight className="h-5 w-5" />
           </button>
           {onClose && (
-            <button aria-label="Close inspector" title="Close inspector" onClick={onClose} className="rounded-xl border border-transparent p-2 text-muted-foreground transition hover:border-border/80 hover:bg-secondary/70 hover:text-foreground">
+            <button
+              aria-label="Close inspector"
+              title="Close inspector"
+              onClick={onClose}
+              className="rounded-xl border border-transparent p-2 text-muted-foreground transition hover:border-border/80 hover:bg-secondary/70 hover:text-foreground"
+            >
               <X className="h-5 w-5" />
             </button>
           )}
         </div>
       </div>
 
-      <div className="flex-1 overflow-y-auto p-5">
+      <div className="flex-1 overflow-y-auto p-4">
         {children || (
           <div className="metis-empty min-h-[260px] border-dashed bg-transparent">
             <p className="text-sm">Select an item to view details</p>
@@ -59,11 +68,17 @@ export function RightPanel({ children, title = 'Details', onClose }: RightPanelP
         )}
       </div>
     </aside>
-  )
+  );
 }
 
 // Pre-built sections for the right panel
-export function MetadataSection({ title, children }: { title: string; children: React.ReactNode }) {
+export function MetadataSection({
+  title,
+  children,
+}: {
+  title: string;
+  children: React.ReactNode;
+}) {
   return (
     <div className="mb-6 rounded-2xl border border-border/70 bg-secondary/45 p-4">
       <h3 className="mb-3 text-xs font-semibold uppercase tracking-[0.22em] text-muted-foreground">
@@ -71,24 +86,34 @@ export function MetadataSection({ title, children }: { title: string; children:
       </h3>
       <div className="space-y-2">{children}</div>
     </div>
-  )
+  );
 }
 
-export function MetadataItem({ label, value }: { label: string; value: React.ReactNode }) {
+export function MetadataItem({
+  label,
+  value,
+}: {
+  label: string;
+  value: React.ReactNode;
+}) {
   return (
     <div className="flex items-start justify-between gap-3 py-1.5">
       <span className="text-sm text-muted-foreground">{label}</span>
-      <span className="text-right text-sm font-semibold text-foreground">{value}</span>
+      <span className="text-right text-sm font-semibold text-foreground">
+        {value}
+      </span>
     </div>
-  )
+  );
 }
 
 export function TagList({ tags }: { tags: string[] }) {
   return (
     <div className="flex flex-wrap gap-1">
       {tags.map((tag) => (
-        <Badge key={tag} variant="neutral">{tag}</Badge>
+        <Badge key={tag} variant="neutral">
+          {tag}
+        </Badge>
       ))}
     </div>
-  )
+  );
 }
